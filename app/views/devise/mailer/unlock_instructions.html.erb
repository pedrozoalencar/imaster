<p><%= t('devise.titles.unlock_instructions',@resource.email) %></p>

<p><%= t('devise.unlock.your_account_is_locked') %></p>

<p><%= t('devise.unlock.link_unlock_account') %></p>

<p><%= link_to t('devise.actions.unlock_my_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
